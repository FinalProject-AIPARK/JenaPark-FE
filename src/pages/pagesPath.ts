const pagesPath = {
  home: '',
  signin: 'signin',
  signup: 'signup',
};

export const pagesFullPath = {
  home: '/',
  signin: `/${pagesPath.signin}`,
  signup: `/${pagesPath.signup}`,
};

export default pagesPath;
